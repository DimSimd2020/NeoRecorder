# NeoRecorder

🎬 Высокопроизводительное приложение для записи экрана с поддержкой до 240 FPS и аппаратным ускорением.

![NeoRecorder](app_icon.png)

## ✨ Возможности

- **Высокая производительность** — запись до 240 FPS с аппаратным ускорением (NVIDIA NVENC, Intel QuickSync, AMD AMF)
- **Гибкая область захвата** — полный экран, выбранная область или конкретное окно
- **Захват аудио** — микрофон и системные звуки
- **Современный интерфейс** — киберпанк дизайн на CustomTkinter
- **Уведомления Windows** — информирование о завершении записи
- **Компактный виджет** — плавающая панель управления во время записи

## 🚀 Установка

### Готовый установщик
Скачайте последний релиз из [Releases](https://github.com/DimSimd2020/NeoRecorder/releases) и запустите `NeoRecorder_Setup.exe`.

### Из исходного кода
```bash
# Клонирование
git clone https://github.com/DimSimd2020/NeoRecorder.git
cd NeoRecorder

# Установка зависимостей
python build.py --deps

# Запуск
python main.py
```

## 🔧 Требования

- Windows 10 (20H1 или новее для ddagrab) / Windows 11
- Python 3.9+ (для разработки)
- FFmpeg (встроен в сборку)
- Видеокарта с поддержкой аппаратного кодирования (опционально)

## 📦 Сборка

```bash
# Сборка EXE
python build.py

# Создание установщика (требуется Inno Setup)
iscc setup_script.iss
```

## 🎯 Использование

1. Запустите NeoRecorder
2. Выберите режим записи: Экран / Область / Окно
3. Настройте аудио источники при необходимости
4. Откройте настройки для выбора FPS и качества
5. Нажмите кнопку записи
6. Для остановки используйте плавающий виджет

## ⚙️ Настройки

| Параметр | Значения | Описание |
|----------|----------|----------|
| FPS | 30, 60, 120, 144, 240 | Частота кадров записи |
| Качество | Быстрая, Баланс, Качество, Без потерь | Профиль кодирования |
| Кодек | Auto | Автоопределение лучшего (NVENC > QSV > AMF > x264) |

## 📁 Структура проекта

```
NeoRecorder/
├── main.py              # Точка входа
├── config.py            # Конфигурация
├── build.py             # Скрипт сборки
├── setup_script.iss     # Inno Setup скрипт
├── core/
│   ├── recorder.py      # Логика записи
│   ├── audio_manager.py # Управление аудио
│   └── window_finder.py # Поиск окон
├── gui/
│   ├── app.py           # Главное окно
│   ├── widgets.py       # UI компоненты
│   └── overlay.py       # Выбор области
├── utils/
│   ├── ffmpeg_handler.py    # FFmpeg интерфейс
│   └── notifications.py     # Windows уведомления
└── assets/
    ├── icons/           # Иконки
    └── lang/            # Локализации
```

## 📄 Лицензия

MIT License

## 👤 Автор

Antigravity Cybernetics
